{
  "name": "attendance",
  "version": "1.0.0",
  "description": "Student Attendance System and AI Exam Generation",
  "private": true,
  "scripts": {
    "install:all": "npm install --workspaces",
    "setup:db": "npm run prisma:generate -w backend && npm run prisma:migrate -w backend && npm run prisma:seed -w backend",
    "dev:backend": "npm run dev -w backend",
    "dev:frontend": "npm run dev -w frontend",
    "build:backend": "npm run build -w backend",
    "build:frontend": "npm run build -w frontend"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "devDependencies": {
    "@types/node": "^25.6.0"
  },
  "dependencies": {
    "dotenv": "^17.4.2"
  }
}
